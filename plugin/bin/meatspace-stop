#!/bin/bash
# Meatspace — stop the background tracker

PID_FILE="/tmp/meatspace-tracker.pid"

if [ -f "$PID_FILE" ]; then
  pid=$(cat "$PID_FILE")
  if kill -0 "$pid" 2>/dev/null; then
    kill "$pid" 2>/dev/null
  fi
  rm -f "$PID_FILE"
fi

rm -f /tmp/meatspace-state.json
